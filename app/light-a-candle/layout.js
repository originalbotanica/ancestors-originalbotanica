export const metadata = {
  title: 'Light a Candle — Ancestors · Original Botanica',
  description: 'Honor a loved one with a perpetual virtual memorial candle on the ancestor altar — from Original Botanica in the Bronx.',
};

export default function LightACandleLayout({ children }) {
  return children;
}
